#include <iostream>

using namespace std;

int main(int argc, char* argv[]){
    int len = 0;
    char str[1024] = {0};
    cin >> len;
    for(int i = 0; i < len; i++){
        cin >> str[i];
    }
    int i = 0;
    int next[1024] = {0};
    next[0] = 0;
    int j = 0;
    while(i < str[0])
    return 0;
}